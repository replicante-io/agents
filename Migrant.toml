# Required, do not edit
database_type = "sqlite"

# Required: Absolute or relative path to your database file.
#           If a relative path is provided, it will be assumed
#           to be relative to this config file dir: `base/src/store/backend/sqlite3/migrations/`
# ex.) database_name = "db/db.db"
database_path = "devtools/base-agent.db"

migration_location = "base/src/store/backend/sqlite3/migrations"
